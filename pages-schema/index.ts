/**
 * Ponto de entrada do esquema de páginas do subsistema DHL.
 */

export { DHL_PAGES_MANIFEST, DHL_ROUTE_PREFIX } from "./manifest";
export type { PageSchema, RouteSegment, RouteFolderSchema } from "./types";
